<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= content_for?(:title) ? yield(:title) : "Document" %></title>
  <style type="text/css">
    body { font-family: Arial, sans-serif; }
    h1 { font-size: 24px; }
    p { font-size: 14px; }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
